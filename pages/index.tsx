import React from 'react';

import {Container} from '../components/container';

const Home = () => (
    <Container>
        <main>
            <div>{'Content goes here'}</div>
        </main>
    </Container>
);

export default Home;
